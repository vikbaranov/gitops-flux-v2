apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cert-manager
  namespace: cert-manager
spec:
  interval: 15m
  timeout: 2m
  chartRef:
    kind: OCIRepository
    name: cert-manager
  install:
    crds: Create
  upgrade:
    crds: CreateReplace
