# Shared infrastructure configuration consumed by env overlays.
# Examples once you have controllers installed: ClusterIssuers, StorageClasses,
# DNS / cluster-wide NetworkPolicies, default IngressClass.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
